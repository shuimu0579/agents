# Audit Findings

**Run**: audit 20260909-092658 | **Scope**: whole repo (16 shell + 27 markdown + config, 57 tracked files / ~8200 lines) | **Audit type**: full (9 dimensions)
**Model**: gpt-6-astra | **Effort**: low | **Audit thread**: 01a083bb-807e-7142-86a4-c793bd686921 | **Job**: audit-mtteozwp-7q4lpk
**Status values**: open | fixed | not-fixed | partial | regressed | skipped (severity filter) | skipped (user stop)
**Closed 2026-09-10**: 48 fixed, 3 recorded as accepted risk. Merged in PRs #1 (`832b0de`) and #2 (`81bf166`).
**Verification**: rows marked VERIFIED were re-checked against the source by the main session; PLAUSIBLE rows were not independently confirmed.

| # | File | Line | Severity | Dimension | Finding | Suggested fix | Status | Round |
|---|------|------|----------|-----------|---------|---------------|--------|-------|
| 1 | hooks/restrict-bash-by-agent.sh | 287 | Critical | 2 | VERIFIED. Snapshot-approval check runs on `$norm` (quotes intact) but quote-stripped `$_norm_clean` is only built later at line 300. `playwright test --update-"snapshots"` misses the `--update-snapshots` regex, falls through to the allowlist elif, and is allowed — the shell then passes `--update-snapshots` to Playwright. One-shot approval bypassed. | Tokenize once with a shell-aware parser (strip quotes/backslashes) BEFORE the approval branch; apply approval policy to the resulting argv, not the raw string. | fixed (3f66bdc) | 1 |
| 2 | hooks/restrict-bash-by-agent.sh | 300 | Critical | 2 | VERIFIED. `deny-git-output` matches only `--output=`. `git diff --output /path/to/protected` uses the separated form git accepts and is allowed, giving arbitrary file write through a "read-only" git allowlist entry. | Reject both attached and separated forms (`--output=X` and `--output X`), plus unambiguous abbreviations, by parsing argv instead of regex-matching the command string. | fixed (3f66bdc) | 1 |
| 3 | hooks/restrict-bash-by-agent.sh | 303 | Critical | 2 | VERIFIED. codegen output regex requires `-o` followed by space or `=`. `playwright codegen -o/tmp/target <url>` (attached short-option value, valid commander.js syntax) is not matched. `--save-storage` and `--save-har` are unrestricted entirely. | Parse the full codegen option grammar; constrain every output path including attached short-option values; add `--save-storage` / `--save-har` to the write-option deny set. | fixed (3f66bdc) | 1 |
| 4 | hooks/restrict-bash-by-agent.sh | 298 | Critical | 2 | VERIFIED (regex scope) / PLAUSIBLE (impact). `playwright test` is allowed with no output-directory restriction — only codegen output is denied. Playwright recursively clears its output directory before a run, so `playwright test --output=hooks` can delete the enforcement layer itself. | Constrain artifact/output directories to a dedicated disposable path and enforce that boundary at the filesystem level, not by string matching. | fixed (700df16) | 1 |
| 5 | hooks/restrict-bash-by-agent.sh | 298 | Critical | 2 | VERIFIED (regex scope). The allowlist permits `playwright test`, but the agent can Write/Edit the specs and config that Playwright then executes as Node code — arbitrary `child_process` with the same privileges as the hooks constraining it. Initial repo-trust attestation does not bind code generated afterwards. | Run Playwright in an OS sandbox with the fleet root, `~/.claude` settings, credentials, and the approval store inaccessible; permit writes only to the assigned checkout and artifact dir. | fixed (700df16) | 1 |
| 6 | hooks/restrict-bash-by-agent.sh | 298 | Critical | 2 | VERIFIED (regex scope) / PLAUSIBLE (exploit path). `git (status\|diff\|log\|show)` is allowed wholesale; `git diff --ext-diff --no-index` runs a configured external diff/textconv helper, and `.git/config` is writable through the mutator gate (see #7). Arbitrary command execution. | Route git through a trusted wrapper with `--no-ext-diff`, textconv, pagers and fsmonitor disabled; protect `.git/config`; reject options that re-enable helpers. | fixed (700df16) | 1 |
| 7 | hooks/restrict-mutator-write.sh | 130-150 | High | 2 | VERIFIED. Self-protection covers `hooks/`, `tests/`, `scripts/`, `$CLAUDE_HOME/hooks`, `$CLAUDE_HOME/scripts`, `$CLAUDE_HOME/settings*.json` — but NOT `~/.claude/rules/` (the live policy registry), NOT project-local `.claude/settings.json`, NOT `.git/config`. A mutator agent can rewrite the rules that govern it. | Add `${CLAUDE_HOME}/rules`, project `.claude/settings*.json`, and `.git/config` to the protected prefix set; prefer an allowlist of assigned product paths over a denylist. | fixed (0c2fb67) | 1 |
| 8 | hooks/restrict-bash-by-agent.sh | 85-92 | High | 2 | VERIFIED. When jq is absent the hook trusts a raw `"agent_type"` regex. A payload spelling the key with a JSON escape (`"agent_type":"_xixi"`) is valid JSON but does not match, so the event is treated as unattributed main-session and allowed. Fail-open under missing dependency. | Delete the regex attribution fallback. Deny any non-empty hook event when the required JSON parser is unavailable. | fixed (3f66bdc) | 1 |
| 9 | hooks/restrict-bash-by-agent.sh | 79 | High | 2 | VERIFIED. `jq -e .` validates JSON truthiness, not the event schema — input `[]` passes, field extraction is swallowed by `\|\| true`, and the hook exits 0. Same shape in the general write hook. | Require a single object with validated event/tool/attribution/tool_input types; reject extraction failures and unexpected shapes with exit 2. | fixed (0c2fb67) | 1 |
| 10 | hooks/restrict-bash-by-agent.sh | 257 | High | 2 | VERIFIED. Effective-config detection handles `--config=` and `--config <path>` only. Playwright supports `-c <file>`; `playwright test -c /tmp/uninspected.ts` is allowed with the config never validated. Repeated options and config-directory args are also unresolved. | Parse the supported CLI grammar and resolve exactly the effective config (including `-c`, repeats, and directory forms) before authorizing. | fixed (3f66bdc) | 1 |
| 11 | hooks/restrict-bash-by-agent.sh | 169-190 | High | 2 | VERIFIED. Config baseURL validation regex-scans source lines; finding one URL on a `baseURL:` line counts as validation. `baseURL: target // http://localhost:3000` passes with `target` unresolved; quoted keys and imported config evade it entirely. | Require a constrained trusted config format (e.g. JSON) for the attested destination, and enforce destination restrictions independently of arbitrary config JavaScript. | fixed (3f66bdc) | 1 |
| 12 | hooks/xixi/restrict-write.sh | 60 | High | 2 | PLAUSIBLE. For input `[]` an unchecked `jq -r` assignment exits 5 under `set -e` — a non-blocking hook error rather than the required exit-2 denial. | Route all parsing and unexpected failures through `deny`, converting any non-zero initialization/extraction status to exit 2. | fixed (0c2fb67) | 1 |
| 13 | hooks/restrict-mutator-write.sh | 95 | High | 2 | PLAUSIBLE. Canonicalization failure blocks only when `agent` is non-empty, but the stated rule is "always deny Write/Edit of hooks/approvals". With python3 unavailable, an unattributed traversal path can skip the approval-directory check. | Require successful canonicalization for every path under unconditional approval protection, including main-session writes. | fixed (0c2fb67) | 1 |
| 14 | e2e-runner.md | 70-95, 107, 161, 179 | High | 3 | PLAUSIBLE — NOT independently verified (Playwright not installed here). Codex reports `playwright test` has no `--base-url` CLI option, yet the agent mandates `--base-url=<literal>` on every invocation, so every compliant run would fail before testing. Verify with `npx playwright test --help`. | If confirmed: pass the attested URL through a validated config (or `PLAYWRIGHT_TEST_BASE_URL`), and update the agent body, the hook's `--base-url` handling, and tests together. | fixed (25b089f) | 1 |
| 15 | code-reviewer.md | 190 vs 63 | High | 5 | VERIFIED. "Zero findings → APPROVE → GO" has no evidence gate, contradicting line 63's NEEDS_INPUT rule for stale/unverifiable scope. A review with unavailable test evidence can emit GO. | Make zero findings produce GO only when all material evidence requirements are satisfied; otherwise NEEDS_INPUT. | fixed (25b089f) | 1 |
| 16 | security-reviewer.md | 144 vs 85 | High | 5 | VERIFIED. Same contradiction: line 85 requires NEEDS_INPUT for material unknowns; line 144's zero-findings rule emits APPROVE/GO regardless. Unknown runtime controls become approval. | Give evidence gaps precedence over the zero-finding template; define the corresponding domain status explicitly. | fixed (25b089f) | 1 |
| 17 | code-reviewer.md | 63 | High | 3 | VERIFIED. "one or more paths unreadable/deleted → NEEDS_INPUT" makes every intentional-deletion review return NEEDS_INPUT, even when the supplied patch contains the removed content. | Distinguish expected deletions in the change set from unexpectedly missing files; review expected deletions from the supplied baseline/diff. | fixed (25b089f) | 1 |
| 18 | tests/hooks.test.sh | 160, 167 | High | 7 | PLAUSIBLE. The stale-token and wrong-mode approval tests omit `BASE_URL`, so the production guard denies them before approval expiry is ever evaluated. Both stay green even if the approval checks were deleted. | Supply valid target attestation, then assert the approval-specific denial rule and token lifecycle. | fixed (25b089f) | 1 |
| 19 | scripts/copy-prompt.sh | 15 | High | 3 | PLAUSIBLE. `stat -f %Su ... \|\| stat -c %U` keeps the BSD-first fallback fixed elsewhere; GNU stat can emit output before failing, contaminating `owner` and rejecting valid Linux clipboard backends. | Select stat syntax by platform, capture only successful output, and add a direct Linux backend-selection test. | fixed (25b089f) | 1 |
| 20 | hooks/lib/security.sh | 123 | Medium | 3 | PLAUSIBLE. `rm -f ... \|\| true` followed by unconditional success reports an approval as consumed even when the token could not be deleted — replay window. | Require successful removal before granting authorization; report cleanup failure explicitly. | fixed (e286ea4) | 1 |
| 21 | hooks/lib/security.sh | 96 | Medium | 2 | VERIFIED (design). Approval tokens are named only `with-deps` / `snapshots` with no session, repo, command, or target binding. A concurrent run can consume an approval intended for a different operation. | Bind tokens to session id + repo root + normalized-command digest; create exclusively and verify all fields on consume. | fixed (79256df) | 1 |
| 22 | hooks/lib/xixi.sh | 70 | Medium | 2 | PLAUSIBLE. The reserved descriptor is closed before the host performs the Write, so the write is not bound to the validated inode — the documented replacement race remains. | Move the write into a trusted helper that owns the descriptor, or enforce the destination boundary with an OS sandbox. | not-fixed — accepted residual risk (ADR 0003): binding the write to the validated fd needs a host API that does not exist | - |
| 23 | hooks/lib/xixi.sh | 129 | Medium | 2 | PLAUSIBLE. The shell fallback when python3 is unavailable re-opens the path after separate symlink/link-count/size checks, losing the Python branch's single-descriptor protection. | Refuse to copy when the safe backend is unavailable; emit the established manual-copy fallback status. | fixed (e286ea4) | 1 |
| 24 | hooks/lib/xixi.sh | 93 | Medium | 3 | PLAUSIBLE. `os.open(path, flags)` runs before the file-type check and omits `O_NONBLOCK`; a FIFO substituted after Write blocks the PostToolUse hook. | Open with `O_NONBLOCK` and no-follow, validate the descriptor, and reject special files before reading. | fixed (e286ea4) | 1 |
| 25 | hooks/lib/core.sh | 23 | Medium | 2 | PLAUSIBLE. Audit output appends to a predictable default temp log with no symlink, ownership, or link-count validation. | Create the log in an owner-only directory; open with no-follow plus ownership checks. | fixed (e286ea4) | 1 |
| 26 | tests/guardrails.sh | 48 | Medium | 4 | PLAUSIBLE. `grep -m1 '^field:'` scans the whole file rather than parsing the YAML frontmatter block, so body text or duplicate keys can satisfy a check while the runtime reads a different value. | Parse only the leading YAML block; reject duplicate keys; validate required types against the fleet contract. | fixed (2ae6130) | 1 |
| 27 | tests/guardrails.sh | 194 | Medium | 7 | PLAUSIBLE. Verdict checks assert only that `**Verdict:**` exists after the first Handoff — not the allowed vocabulary, nor that it is the final line. | Assert the exact `GO \| BLOCK \| NEEDS_INPUT` vocabulary, final-line position, and domain-status mapping per template. | fixed (2ae6130) | 1 |
| 28 | tests/guardrails.sh | 249 | Medium | 7 | PLAUSIBLE. Recursive discovery skips `./docs/*`, `./tests/*`, `./templates/*`, `./hooks/*` despite documenting recursive runtime discovery; an agent definition added under those paths escapes the fleet check. | Inspect every runtime-discoverable Markdown file for agent frontmatter regardless of directory. | fixed (2ae6130) | 1 |
| 29 | tests/guardrails.sh | 31 | Medium | 7 | PLAUSIBLE. The output-contract comparison defaults to `$HOME/.claude/agents/docs/...`; on a clean CI checkout it can validate the fixture while skipping the repo's own `docs/agent-output-contract.md`. | Always compare the checked-out canonical contract against the fixture; make installed-copy validation a separate check. | fixed (2ae6130) | 1 |
| 30 | tests/hook-e2e.test.sh | 47 | Medium | 7 | PLAUSIBLE. Registration checks accept any command string containing `restrict-bash-by-agent.sh` — `echo restrict-bash-by-agent.sh` passes. Write registration accepts a Write-only matcher while claiming Write/Edit coverage. | Validate executable command resolution and matcher coverage; exercise both Write and Edit through the registered chain. | fixed (c7d0754) | 1 |
| 31 | tests/xixi-hooks.test.sh | 58 | Medium | 7 | PLAUSIBLE. Tests `chmod +x` the copied hooks, masking lost executable bits on the originals; CI directly checks only the two general gates. | Assert executable bits on the original xixi hooks and clipboard backend before copying; test direct invocation. | fixed (c7d0754) | 1 |
| 32 | tests/xixi-hooks.test.sh | 216 | Medium | 7 | PLAUSIBLE. No tests for missing jq/python3, wrong JSON types, missing library functions, empty/oversized output, or a failing clipboard backend. | Add an explicit dependency/failure matrix asserting denial, fallback status, cleanup, and absence of clipboard writes. | fixed (c7d0754) | 1 |
| 33 | tests/hooks.test.sh | 66 | Medium | 7 | PLAUSIBLE. `env ... "$@" bash` inherits `APPROVAL_DIR`, `HOOK_LIB_DIR` and target vars; an exported approval directory can redirect "isolated" tests at real pending approvals. | Clear security-sensitive inherited variables; set every hook dependency, approval path, target, and config path explicitly to fixtures. | fixed (c7d0754) | 1 |
| 34 | hooks/restrict-mutator-write.sh | 106-132 | Medium | 6 | VERIFIED (code shape) / PLAUSIBLE (timing). `uname -s` and case-folding subprocesses run per protected prefix, alongside repeated python3/jq invocations. Codex measured a ~872 ms median for an ordinary write decision against a 5 s timeout; this is on the PreToolUse hot path. | Cache platform and canonical roots, parse JSON once, normalize once, compare all prefixes in a single process. | fixed (a5928c7) | 1 |
| 35 | .codex/config.toml | 4 | Medium | 8 | VERIFIED. `npx -y claude-octopus@1.2.0` fetches and executes a package at runtime with no lockfile or integrity pin for its transitive dependencies. | Install the audited server from a locked dependency tree and point the config at an explicit local executable. | not-fixed — upstream: cc-suite:update owns the entry and re-renders it; risk documented in AGENTS.md | - |
| 36 | .github/workflows/agents-ci.yml | 14 | Medium | 8 | PLAUSIBLE. `actions/checkout@v4` is a mutable tag, `permissions:` is unspecified (inherits broad defaults), and checkout credentials persist by default. The e2e template repeats mutable action refs. | Pin actions to reviewed commit SHAs, set `permissions: contents: read`, set `persist-credentials: false`. | fixed (1048eba) | 1 |
| 37 | .gitignore | 1 | Medium | 8 | VERIFIED via `git check-ignore`: `hooks/approvals/with-deps`, `hooks/approvals/snapshots`, and `.env` are all NOT ignored. A live approval token or secrets file can be committed. | Ignore `hooks/approvals/*` except its README, ignore `.env*` except examples, and ignore generated Playwright auth/artifact files. | fixed (1048eba) | 1 |
| 38 | docs/agent-output-contract.md | 74 | Medium | 5 | PLAUSIBLE. The canonical skeleton places `**Verdict:**` near the top, contradicting line 29's "ends with exactly one canonical verdict line" and the agents' terminal-verdict templates. | Move the skeleton's verdict after Handoff and synchronize `tests/fixtures/output-contract.md`. | fixed (1e36c47) | 1 |
| 39 | docs/agent-output-contract.md | 76 | Medium | 5 | VERIFIED. "APPROVE MUST be bound to the reviewed SHA" contradicts `code-reviewer.md:135`, which accepts "patch hash, base/head pair, or paths + diff snapshot timestamp". | Define one stable scope-identifier contract and use it in both reviewers, shared policy, and fixtures. | fixed (1e36c47) | 1 |
| 40 | ~/.claude/rules/agents.md | 9 | Medium | 5 | VERIFIED. The live rules file says "**5 active agents**" and never mentions `_critical_thinking`, while `AGENTS.md`, `CLAUDE.md` ("6 agents") and `tests/fixtures/agent-contract.tsv` all carry 6. The routing and capability tables omit it entirely, so it is never dispatched by rule. | Update the live registry to six agents, add `_critical_thinking` routing/capability rows, and generate or validate the table against `agent-contract.tsv`. | fixed (1e36c47) | 1 |
| 41 | code-reviewer.md | 97 | Medium | 5 | VERIFIED. "Size and complexity thresholds are review signals, not blocking limits" conflicts with `~/.claude/rules/coding-style.md` ("Keep functions under 50 lines and files under 800 lines") and its mandatory pre-complete checklist. | Decide whether the limits are advisory or enforced, then synchronize the reviewer body and the live rule. | fixed (1e36c47) | 1 |
| 42 | hooks/xixi/CONTRACT.md | 19 | Medium | 5 | VERIFIED. "settings.json is the ONLY loaded config" conflicts with `~/.claude/rules/hooks.md` ("Config: `~/.claude/hooks/hooks.json`"). Maintainers are pointed at different registration sources. | Document the actual host settings hierarchy; remove the obsolete `hooks.json` authority claim from the live rule. | fixed (1e36c47) | 1 |
| 43 | hooks/xixi/CONTRACT.md | 37 | High | 7 | VERIFIED (text). "Live Write-event attribution has not been re-verified" leaves the sandbox's core prerequisite untested — missing attribution means pass-through — and `tests/hook-e2e.test.sh` explicitly does not invoke Claude Code. | Add a real-host installation test that verifies attributed Write events and outside-sandbox denial; fail installation when attribution cannot be established. | fixed (25b089f) | 1 |
| 44 | docs/agents/domain.md | 9 | Medium | 9 | PLAUSIBLE. Domain guidance points at `docs/adr/`, while `adr/0000-*.md:24` explicitly chose `adr/` at the repo root; the missing-path instruction then says to proceed silently. | Point domain guidance at the adopted root `adr/` directory and update its diagrams. | fixed (1e36c47) | 1 |
| 45 | AGENTS.md | 101 | Medium | 9 | PLAUSIBLE. "There is no build or compile step" sits beside a claim that Codex loads a generated `~/.codex/agents/*.toml` mirror, but no generator, sync command, or drift check is tracked. | Document and version the mirror-generation process including capability translation, installation, and drift checks. | fixed (1e36c47) | 1 |
| 46 | AGENTS.md | 7 | Medium | 9 | VERIFIED. Prerequisites list Codex, Bash, optional Playwright — but omit jq, python3, clipboard utilities, and hook registration, all of which the enforcement layer depends on. Dropping the files into the directory does not install enforcement. | Add a reproducible install/preflight procedure checking dependencies, executable bits, hook registration, and real event attribution. | fixed (1e36c47) | 1 |
| 47 | adr/ | 0001:22 | Medium | 9 | VERIFIED. The only implementation ADR covers the xixi restoration. The shared-hook architecture, approval trust model, the decision to run Playwright unsandboxed, and the Codex mirror have no ADR, despite `AGENTS.md` requiring structural choices to be recorded. | Add focused ADRs for the enforcement boundary, accepted residual risk, alternatives considered, and executable acceptance checks. | fixed (1e36c47) | 1 |
| 48 | docs/audits/grill-report-2026-08-18.md | 174 | Medium | 9 | PLAUSIBLE. Claims TOCTOU was "杜绝" (eliminated), contradicting `hooks/xixi/CONTRACT.md:36` ("A concurrent attacker may still race"); also cites a non-existent `tests/lib/assert.sh` as evidence. | Append a dated correction separating historical claims from currently verified guarantees. | fixed (1e36c47) | 1 |
| 49 | hooks/xixi/restrict-write.sh | 84 | Low | 1 | PLAUSIBLE. `assert_reserved_xixi_file` repeats validation already done by `xixi_reserve_and_validate`, adding another python3 startup without binding the host Write to that inode. | Consolidate reservation/validation into one implementation; address the write boundary separately. | not-fixed — deliberate: a post-condition assertion kept; reasoning at the call site | - |
| 50 | hooks/lib/fs.sh | 33 | Low | 1 | PLAUSIBLE. `fs_get_nlink`, `fs_get_owner`, `fs_get_size` have no callers, while xixi/backend code reimplements the same metadata checks (including the BSD-first stat bug in #19). | Remove the unused helpers or route all metadata checks through them; eliminate the competing implementations. | fixed (a5928c7) | 1 |
| 51 | tests/run_all.sh | 24, 8 | Low | 1 | PLAUSIBLE. `eval "$cmd"` interprets four fixed command strings unnecessarily; `STRICT=1` at line 8 is unused. | Pass command arguments directly to `run_suite` and run `"$@"`; delete the unused variable. | fixed (a5928c7) | 1 |
